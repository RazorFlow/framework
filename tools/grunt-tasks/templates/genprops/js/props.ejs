{
key: '<%= key %>',
<% if(value.isPropertyBase){ -%>
    value:new <%= value['class'] -%>(),
<% } else if(value.isPropertyList) { -%>
    value:new PropertyList(<%= value['class'] -%>),
<% } else if(value['default'] !== undefined) { -%>
    value:<%- JSON.stringify(value['default']) -%>,
<% } else { -%>
    value:null,
<% } %>
    type: '<%= value['type'] -%>'
}