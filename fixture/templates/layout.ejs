<html>
<head>Razordoc example doc</head>
<div class="container">
    <div class="row">
        <div class="col-md-4">
            <ul class="nav">
                <%- render('nav.ejs', {items: tree.findAllClassNames()}) %>
            </ul>
        </div>
        <div class="col-md-8">
            <% if(currentClass != 'index') { %>
                <%- render('class.ejs', {tree: tree, item: tree.findNodeByClassName(currentClass)}) %>
            <% } else { %>
                <%- render('index.ejs', {}) %>
            <% } %>
        </div>
</div>
</html>